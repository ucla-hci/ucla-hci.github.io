## instantiate new project skill
given the submitted-data.csv and the entry of the latest project (identifiable by name that i will specify),
- add a new entry to the projects in data.yml following its existing structure
- fill in as much info as possible from the provided text in the .csv entry
- in addition also need to
    - download the thumbnail image, properly store and refer to it
    - download the full video in a temp dir and i will upload it to vimeo and add a link manually later
    - download all the images and i will upload them to flickr and add a link manually later
    - convert bibtex to html-friendly text using the existing python script
    - convert the first page of the paper into a small snapshot (see other projects' in data.yml as examples)